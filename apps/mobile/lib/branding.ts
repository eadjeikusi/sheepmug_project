import type { ImageSourcePropType } from "react-native";

/** Shepherd + sheep logo (squircle); used on splash, boot loading, and login. */
export const SHEEPMUG_LOGO: ImageSourcePropType = require("../assets/sheepmug-logo.png");
